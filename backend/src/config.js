import 'dotenv/config';
const required=['SUPABASE_URL','SUPABASE_ANON_KEY','SUPABASE_SERVICE_ROLE_KEY'];
for(const key of required) if(!process.env[key]) throw new Error(`Falta ${key} en .env`);
const integer=(name,fallback)=>{const value=Number(process.env[name]??fallback);if(!Number.isFinite(value))throw new Error(`${name} invalida`);return value};
export const config={nodeEnv:process.env.NODE_ENV||'development',port:integer('PORT',4000),frontendUrls:(process.env.FRONTEND_URL||'http://localhost:5173').split(',').map(v=>v.trim()).filter(Boolean),supabaseUrl:process.env.SUPABASE_URL,anonKey:process.env.SUPABASE_ANON_KEY,serviceKey:process.env.SUPABASE_SERVICE_ROLE_KEY,loginMax:integer('LOGIN_MAX_ATTEMPTS',5),loginWindow:integer('LOGIN_WINDOW_MINUTES',15),generalLimit:integer('GENERAL_RATE_LIMIT',300),registrationUrl:process.env.REGISTRATION_PUBLIC_URL||'http://localhost:5173/registro',signedTtl:integer('SIGNED_URL_TTL_SECONDS',300),maxUpload:integer('MAX_UPLOAD_BYTES',5242880),waVersion:process.env.WHATSAPP_GRAPH_VERSION||'v23.0',waPhoneId:process.env.WHATSAPP_PHONE_NUMBER_ID||'',waToken:process.env.WHATSAPP_ACCESS_TOKEN||'',waVerify:process.env.WHATSAPP_VERIFY_TOKEN||'',waSecret:process.env.WHATSAPP_APP_SECRET||'',cronSecret:process.env.CRON_SECRET||'',scheduler:process.env.ENABLE_SCHEDULER==='true'};
